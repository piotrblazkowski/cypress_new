{
  "devDependencies": {
    "cypress": "^13.3.1",
    "prettier": "3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "test": "npx cypress run --spec 'cypress/e2e/my/test.cy.js' --env allure=true",
    "report": "npx allure generate allure-result --clean -o allure-report"
  }
}
